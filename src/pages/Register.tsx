import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { supabase } from '../supabaseClient';
import Navbar from '../components/Navbar';
import Footer from '../components/Footer';
import { getErrorMessage } from '../utils/errorHandling';

export default function Register() {
  const navigate = useNavigate(); const [fullName, setFullName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [errorMsg, setErrorMsg] = useState<string | null>(null); const [successMsg, setSuccessMsg] = useState<string | null>(null); const [loading, setLoading] = useState(false);
  const handleRegister = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setErrorMsg(null); setSuccessMsg(null); try { const targetEmail = email.trim().toLowerCase(); const { data, error } = await supabase.auth.signUp({ email: targetEmail, password, options: { data: { display_name: fullName.trim() } } }); if (error) throw new Error(error.message.toLowerCase().includes('already registered') ? 'This email address is already registered.' : error.message); if (data?.user) await supabase.from('profiles').upsert([{ id: data.user.id, name: fullName.trim(), email: targetEmail }]); setSuccessMsg('Account created. Redirecting to your collection...'); setTimeout(() => navigate('/'), 1500); } catch (err) { setErrorMsg(getErrorMessage(err)); } finally { setLoading(false); } };
  return <div className="min-h-screen flex flex-col bg-brand-secondary font-sans text-brand-ink"><Navbar /><main className="flex flex-1 items-center justify-center px-5 py-12"><div className="grid w-full max-w-5xl overflow-hidden border border-brand-line bg-white shadow-xl md:grid-cols-[.85fr_1.15fr]"><aside className="hidden bg-brand-primary p-10 text-brand-secondary md:flex md:flex-col md:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.3em] text-brand-secondary/70">Meenakkhi / The collection</p><h2 className="mt-20 max-w-xs font-serif text-5xl leading-[1.05]">Make room for beautiful things.</h2></div><p className="max-w-xs border-t border-brand-secondary/20 pt-5 text-sm leading-6 text-brand-secondary/70">Save your favourites, follow new arrivals, and keep every occasion close.</p></aside><section className="p-7 sm:p-12"><div className="mb-9"><p className="text-[10px] font-bold uppercase tracking-[.25em] text-brand-accent">Begin your edit</p><h1 className="mt-3 font-serif text-4xl text-brand-primary-dark">Create an account</h1><p className="mt-2 text-sm leading-6 text-brand-primary/70">A quieter way to discover the saree meant for you.</p></div>{errorMsg && <div className="mb-5 border border-brand-accent/40 bg-brand-accent/10 p-3 text-xs font-semibold text-brand-primary-dark">{errorMsg}</div>}{successMsg && <div className="mb-5 border border-brand-primary/30 bg-brand-secondary p-3 text-xs font-semibold text-brand-primary-dark">{successMsg}</div>}<form onSubmit={handleRegister} className="space-y-5"><label className="block"><span className="mb-2 block text-[10px] font-bold uppercase tracking-[.18em] text-brand-primary">Full name</span><input type="text" required value={fullName} onChange={(e) => setFullName(e.target.value)} placeholder="Your name" className="w-full border-b border-brand-line bg-transparent px-0 py-3 text-sm outline-none transition placeholder:text-brand-primary/35 focus:border-brand-accent" /></label><label className="block"><span className="mb-2 block text-[10px] font-bold uppercase tracking-[.18em] text-brand-primary">Email address</span><input type="email" required value={email} onChange={(e) => setEmail(e.target.value)} placeholder="name@example.com" className="w-full border-b border-brand-line bg-transparent px-0 py-3 text-sm outline-none transition placeholder:text-brand-primary/35 focus:border-brand-accent" /></label><label className="block"><span className="mb-2 block text-[10px] font-bold uppercase tracking-[.18em] text-brand-primary">Create password</span><input type="password" required minLength={6} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="At least 6 characters" className="w-full border-b border-brand-line bg-transparent px-0 py-3 text-sm outline-none transition placeholder:text-brand-primary/35 focus:border-brand-accent" /></label><button disabled={loading} className="mt-4 w-full bg-brand-primary-dark px-5 py-4 text-xs font-bold uppercase tracking-[.2em] text-brand-secondary transition hover:bg-brand-primary disabled:opacity-60">{loading ? 'Creating account...' : 'Create account'}</button></form><p className="mt-8 text-center text-sm text-brand-primary/65">Already a member? <button onClick={() => navigate('/login')} className="font-bold text-brand-accent underline underline-offset-4">Sign in</button></p></section></div></main><Footer /></div>;
}
